# Robotic-Arm-Processing
Robotic arm with different approches 

-The first methode using Color detection  ,the programe will detect the object colors and thier container colors based on the selected RGB
 set when pressing the key (space) the programe will store the cordinates in an array and than move each object to its container 
 
